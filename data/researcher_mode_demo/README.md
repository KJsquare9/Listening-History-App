# Researcher Mode Demo Folder

Upload this whole folder in Researcher Mode, or select `metadata.csv` together with the `rm_*.csv` files.

The participant IDs in `metadata.csv` intentionally match the listening-history filenames. Tracks are drawn from `data/songs.csv` so the app can enrich them with valence and arousal values for trajectory and distribution charts.
